# python 3.10+
# 版本范围式声明,避免精确 pin 导致索引找不到
fastapi
uvicorn
python-multipart>=0.0.20
wsproto  # uvicorn websocket 后端 (没装时 WS /invoke/stream 404)

pyyaml>=6.0
requests>=2.31
numpy>=1.26,<3

cryptography>=42.0

# Pydantic v2 (schema_module)
pydantic>=2.5

# 向量库 / embedding (RAG 链路核心)
faiss-cpu>=1.7,<2
sentence-transformers>=2.5

# 文档解析 (document_parser_module, 全部可选, 装了多支持几种格式)
pypdf>=4.0
python-docx>=1.0

# 图片处理 (image_describe / 拖拽图片场景生成测试图)
Pillow>=10.0

# Agent 工具
# (无需额外, 都用 stdlib + requests)
