# python 3.10+
# 版本范围式声明,避免精确 pin 导致索引找不到
fastapi
uvicorn
python-multipart>=0.0.20
wsproto  # uvicorn websocket 后端 (没装时 WS /invoke/stream 404)

pyyaml>=6.0
requests>=2.31
numpy>=1.26,<3

cryptography>=42.0

# Pydantic v2 (schema_module)
pydantic>=2.5

# 向量库 / embedding (RAG 链路核心)
faiss-cpu>=1.7,<2
sentence-transformers>=2.5

# 文档解析 (document_parser_module, 全部可选, 装了多支持几种格式)
pypdf>=4.0
python-docx>=1.0

# 图片处理 (image_describe / 拖拽图片场景生成测试图)
Pillow>=10.0

# Agent 工具核心: 无需额外 (都用 stdlib + requests)
# 可选工具依赖 (缺则该工具优雅降级 MISSING_DEPS, 不影响启动):
psutil>=5.9        # system_info — 只读看本机 CPU/内存/磁盘/网络/进程 (用于"直接查看电脑使用情况")
# pyautogui>=0.9   # computer_use — 控制真实桌面 (须有显示环境; Linux 另需 scrot + python3-tk), 按需装
