# Claude Code 使用补遗 · Windows/中文/conda 实战篇

> 定位：官方/通用的 `CLAUDE-CODE-TIPS.md` 讲的是**方法论骨架**（SessionStart/Stop Hook、记忆文件四件套、技能化、改前验证、红线、回归测试、上下文瘦身、三层治理、能自动的自动化）。
> 本文是**在本项目实际使用中沉淀、但通用文档里没有**的增量经验——绝大多数是"在 Windows + 中文 + conda 这套具体环境里，哪些坑会把正常改动伪装成 bug"，以及一套更严的协作纪律。

---

## 一、Windows 环境陷阱（通用文档完全没有，最实用）

| 坑 | 现象 | 正确做法 |
|---|---|---|
| **Python 解释器** | PATH 上的 `python` 是坏 stub，依赖不全、pytest 跑不起来 | 固定用 conda base 绝对路径 `C:\ProgramData\miniconda3\python.exe`，依赖齐、可跑全量 pytest |
| **起服务** | 用 Bash 工具起服务时，Git Bash 把 coreutils 的 `dir`/`find` 灌进 `shell_exec` 的 PATH，污染运行环境 | 用 PowerShell / `start_dev.bat`（走 Windows PATH）起服务 |
| **验证中文接口** | Git-Bash 的 curl 发 GBK、PS5.1 把响应按 Latin-1 解码，把编码问题伪装成"服务端 bug" | 用浏览器 `fetch` 或 python `requests` 验证中文接口 |
| **脚本批改文件** | 工作树是 CRLF；脚本直接写会产生 `\r\r\n` 翻倍，把文件改坏 | 读用 universal 换行、写用 `newline=''` 手动拼 `\r\n` |
| **跨工具传中间文件** | `/tmp` 在 Python 和 Git Bash 里解析到**不同目录** | 一律用显式绝对路径传中间文件 |
| **改前端静态资源** | 改完 JS/CSS 浏览器吃旧缓存，改了等于没改 | 必须 bump `index.html` 里的 `?v=NNN` 做缓存击穿 |

---

## 二、协作 / 决策偏好（比文档"红线"更细的做事方式）

- **报问题必带根因 + 可落地解法**：不能只说"坏了"，要说清"为什么会有这个问题"和"怎么解决"。
- **多解法按"根治"选型，不按"最小改动"选型**：范围上守最小改动，深度上选治本。
- **最小改动原则**：只动对应功能，禁"顺带/顺手"改别的；改完要验证旁边没被带倒。
- **能力按内容判定，不维护枚举白名单**：类型支持看内容/能力，别搞"换个后缀就改一次代码"的枚举清单。
- **已完成的任务不重读**：下次直接用既有上下文/结论往下推。
- **任务三段式**：先列计划清单 → 过程有记录 → 结果有反馈。

---

## 三、Git 操作习惯（文档没提）

- **提交后自动 `git push origin main`**，不再逐次询问（疑似敏感内容例外，仍会先确认）。
- **只显式 `git add` 具体文件，禁用 `git add -A / .`**：工作树里常有未跟踪的运行时产物，全量 add 会误提交。

---

## 四、对文档"记忆四件套"的升级

- **记忆分层**：
  - **全局层 = 使用者画像**（角色、偏好、纪律）——始终注入。
  - **项目层 = 可切换作用域**（`active_project_root`）——按项目挂载。
  - 好处：避免把某个具体项目当成全局默认，污染其他项目的判断。
  - 相比通用文档"单一项目四文件约定"，多了一层作用域切换。

---

## 一句话总结

通用文档教你**怎么搭骨架**（Hook + 记忆 + 技能 + 治理）；本项目的增量几乎全是**"在真实 Windows/中文/conda 环境里，哪些环境差异会把正常改动伪装成 bug"**，再加一套**根治优先 / 最小改动 / 显式 git** 的协作纪律——这些正是通用文档不写、却直接决定实际效率的东西。
